/**
 * Components barrel — single entry point for the entire design system.
 *
 * Re-exports every reusable UI component so consumers can import from
 * a single path:
 *
 *   import { Button, Card, StatCard } from '@/components'
 *
 * Reference: docs/07_UI_UX_GUIDELINES.md §3
 */

export * from './ui'
export * from './cards'
