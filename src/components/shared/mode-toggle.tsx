/**
 * @deprecated Use `ThemeToggle` from `@/components/layout/theme-toggle`.
 * This re-export is kept for backward compatibility so there is a single
 * source of truth for the theme toggle (design-system rule: no duplicate
 * components). It will be removed once call sites migrate.
 */
export { ThemeToggle as ModeToggle } from '@/components/layout/theme-toggle'
